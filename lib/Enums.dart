// Types of activities
enum ActivityType { warmup, cooldown, walk, jog, run, sprint }

// Goals currently supported by the app
enum GoalType { run }

// Subtypes of the supportred goals
enum GoalSubType { five, ten, half, full, ultra, trail }

// Avaliable genders for users
enum Gender { male, female, other }